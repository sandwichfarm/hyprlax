---
phase: 02-astronomical-scene-model
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [examples/pixel-city-dynamic/dynamic_scene.py]
autonomous: true
requirements: [LIGHT-01, LIGHT-02, LIGHT-03, SKY-01, SKY-02]
must_haves:
  truths:
    - "Every requested named lighting state is an exact timeline keyframe with smooth numeric interpolation"
    - "Sun and cross-midnight moon trajectories are bounded and timezone-aware"
    - "Full moon changes night lighting while new moon contributes zero lunar fill"
    - "Layer looks contain only current Hyprlax tint, opacity, and blur primitives"
  artifacts:
    - path: examples/pixel-city-dynamic/dynamic_scene.py
      provides: Pure SceneState model and celestial/lighting calculations
      contains: "def compute_scene_state"
---

<objective>Implement the deterministic astronomical scene model consumed by later asset and IPC phases.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add pure scene types, named keyframes, and continuous layer looks</name>
  <files>examples/pixel-city-dynamic/dynamic_scene.py</files>
  <read_first>examples/pixel-city-dynamic/dynamic_scene.py, .planning/phases/02-astronomical-scene-model/02-CONTEXT.md, .planning/phases/02-astronomical-scene-model/02-RESEARCH.md</read_first>
  <action>Add frozen `LayerLook` and `SceneState` dataclasses, lighting preset constants for exactly night/sunrise/morning/high_noon/late_afternoon/sunset, clamp/lerp/smoothstep helpers, fallback-aware ordered local keyframe construction, and numeric interpolation for six base-layer tint RGB/strength, opacity, blur, ambient brightness, stars opacity, city-light intensity, and `saturation_impression`. Exact keyframe timestamps must report the keyframe label; intermediate labels select the dominant endpoint without changing numeric continuity. Validate aware `now` and convert it into `Astronomy.timezone`.</action>
  <verify>python3 -m py_compile examples/pixel-city-dynamic/dynamic_scene.py</verify>
  <acceptance_criteria>
    - Source defines frozen `LayerLook`, frozen `SceneState`, and `compute_scene_state`.
    - Preset keys are exactly the six requested labels.
    - SceneState contains six `1.png`..`6.png` looks with tint/strength/opacity/blur only.
    - No string/property named plain `saturation` is introduced; descriptive `saturation_impression` exists.
  </acceptance_criteria>
  <done>Any aware instant produces continuous named lighting and six supported layer looks.</done>
</task>
<task type="auto">
  <name>Task 2: Add bounded solar/lunar trajectories and phase-scaled night fill</name>
  <files>examples/pixel-city-dynamic/dynamic_scene.py</files>
  <read_first>examples/pixel-city-dynamic/dynamic_scene.py, .planning/phases/02-astronomical-scene-model/02-RESEARCH.md</read_first>
  <action>Implement normal sun progress/visibility/UV/elevation using the locked formulas and horizon fade; deterministic polar_night (hidden) and midnight_sun (visible cyclic daylight) branches; moon interval selection over rise-1d/rise/rise+1d with set rollover; hide moon when either event is missing; and illumination-scaled moon opacity/lunar fill. Apply lunar lift only to the interpolated night component, cap strength reduction at 0.16, and emit phase/illumination/progress for Phase 3.</action>
  <verify>python3 -m py_compile examples/pixel-city-dynamic/dynamic_scene.py</verify>
  <acceptance_criteria>
    - Sun x is within -0.34..0.34, y within -0.24..0.18, progress/opacity/elevation within 0..1.
    - Moon interval code considers adjacent dates and never invents a missing rise/set.
    - New moon produces `lunar_fill == 0`; full moon while visible produces greater ambient/lunar fill than quarter moon.
    - Polar branches cannot divide by a missing or zero-length daylight interval.
  </acceptance_criteria>
  <done>SceneState carries bounded celestial motion and moon-aware night illumination for normal/polar/cross-midnight inputs.</done>
</task>
</tasks>

<verification>Run py_compile, the existing Phase 1 suite, and git diff --check.</verification>
<output>Create 02-01-SUMMARY.md with formulas, deviations, evidence, and Lore commit.</output>

