---
phase: 02-astronomical-scene-model
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [tests/test_pixel_city_dynamic.py]
autonomous: true
requirements: [LIGHT-01, LIGHT-02, LIGHT-03, SKY-01, SKY-02]
must_haves:
  truths:
    - "Tests cover every named anchor, interpolation continuity, DST, polar inputs, and celestial bounds"
    - "Tests prove rise-after-set moon behavior across midnight and missing-event hiding"
    - "Tests prove new, quarter, and full moons produce ordered lunar lighting"
  artifacts:
    - path: tests/test_pixel_city_dynamic.py
      provides: Direct deterministic Phase 2 model coverage
      contains: "class SceneModelTests"
---

<objective>Lock all scene-model requirements with deterministic numeric tests before visual synthesis.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Test named lighting, continuous interpolation, layer primitives, and DST/polar behavior</name>
  <files>tests/test_pixel_city_dynamic.py</files>
  <read_first>examples/pixel-city-dynamic/dynamic_scene.py, tests/test_pixel_city_dynamic.py, .planning/phases/02-astronomical-scene-model/02-CONTEXT.md</read_first>
  <action>Add `SceneModelTests` with exact aware instants for each named keyframe, midpoint/intermediate states, values one second before/after anchors, UTC-to-Europe/Budapest DST conversion, normal astronomy with missing fields, polar_night, and midnight_sun. Assert continuity epsilon, 0..1 scalar bounds, six exact layer keys, RGB/strength/opacity bounds, preserved base blur intent, and absence of unsupported plain saturation command/property.</action>
  <verify>python3 -m unittest -v tests.test_pixel_city_dynamic.SceneModelTests</verify>
  <acceptance_criteria>
    - All six labels are asserted at deterministic instants.
    - Every anchor has before/after continuity assertions on ambient and at least one layer tint strength.
    - DST and both polar statuses execute without naive-time errors or division by zero.
  </acceptance_criteria>
  <done>Named and intermediate lighting remains continuous, bounded, timezone-aware, and limited to supported layer primitives.</done>
</task>
<task type="auto">
  <name>Task 2: Test solar/lunar bounds, cross-midnight visibility, null events, and illumination ordering</name>
  <files>tests/test_pixel_city_dynamic.py</files>
  <read_first>examples/pixel-city-dynamic/dynamic_scene.py, tests/test_pixel_city_dynamic.py</read_first>
  <action>Add table-driven solar checks from pre-dawn through post-dusk; moonrise 23:00/moonset 09:00 checks at 01:00, noon, and 23:30; missing-rise and missing-set cases; and identical night fixtures at 0%, 50%, and 100% illumination. Assert all position/progress/opacity bounds, opposite daylight endpoints, hidden null-event moon, exact phase preservation, strictly ordered lunar fill/full-moon ambient lift, and no mutation of Astronomy inputs.</action>
  <verify>python3 -m unittest -v tests.test_pixel_city_dynamic.SceneModelTests &amp;&amp; tests/test_pixel_city_dynamic.sh</verify>
  <acceptance_criteria>
    - Cross-midnight moon is visible at 01:00 and 23:30 but hidden at noon.
    - One/null lunar event always hides moon.
    - New, quarter, full fixtures satisfy `lunar_fill(new) == 0 < quarter < full`.
    - All Phase 1 tests remain passing.
  </acceptance_criteria>
  <done>Celestial and lunar-lighting edge cases have direct deterministic evidence.</done>
</task>
</tasks>

<verification>Run full Python suite, script wrapper, py_compile, and git diff --check.</verification>
<output>Create 02-02-SUMMARY.md and proceed to the Phase 2 verifier.</output>
