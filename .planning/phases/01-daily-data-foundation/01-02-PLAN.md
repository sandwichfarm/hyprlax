---
phase: 01-daily-data-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - tests/test_pixel_city_dynamic.py
  - tests/test_pixel_city_dynamic.sh
autonomous: true
requirements:
  - BASE-01
  - BASE-02
  - GEO-01
  - GEO-02
  - ASTRO-01
  - ASTRO-02
  - CACHE-01
  - CACHE-02
must_haves:
  truths:
    - "Automated tests prove a failed same-day attempt is not retried after restart"
    - "Automated tests prove concurrent processes cause exactly one provider fetch"
    - "Provider validation, stale fallback, manual bypass, null polar events, and copied TOML are covered without network"
  artifacts:
    - path: tests/test_pixel_city_dynamic.py
      provides: Deterministic unit/concurrency coverage for every Phase 1 invariant
      contains: "class DailyCacheTests"
    - path: tests/test_pixel_city_dynamic.sh
      provides: Repository script-test entrypoint
      contains: "test_pixel_city_dynamic.py"
  key_links:
    - from: tests/test_pixel_city_dynamic.py
      to: examples/pixel-city-dynamic/dynamic_scene.py
      via: importlib-loaded production module and injected fixtures
      pattern: "dynamic_scene.py"
---

<objective>
Prove the entire Phase 1 boundary with deterministic fixtures, including the concurrency and failed
attempt cases most likely to violate the provider request policy.

Purpose: prevent later visual work from hiding an incorrect or unbounded data foundation.
Output: standard-library unittest suite and executable repository script-test wrapper.
</objective>

<execution_context>
@.planning/phases/01-daily-data-foundation/01-01-PLAN.md
@.planning/phases/01-daily-data-foundation/01-CONTEXT.md
@.planning/phases/01-daily-data-foundation/01-RESEARCH.md
@.planning/codebase/TESTING.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Lock provider/cache and copied-scene behavior with deterministic unit fixtures</name>
  <files>tests/test_pixel_city_dynamic.py</files>
  <read_first>examples/pixel-city-dynamic/dynamic_scene.py, examples/pixel-city-dynamic/parallax.toml, .planning/phases/01-daily-data-foundation/01-RESEARCH.md, .planning/codebase/TESTING.md</read_first>
  <action>Create a Python `unittest` suite that imports the production module by file path, uses `TemporaryDirectory` for cache/XDG state, fixed aware datetimes, and injected call-counting fetchers. Cover: copied TOML parses with six existing layers; first location/astronomy success; same-day restart makes zero new calls; same-day failure then restart makes one total call; stale previous success survives next-day failure; manual coordinates make zero ip-api calls; changed manual location on the same date makes zero new astronomy calls and returns neutral fallback instead of wrong-place cache; malformed JSON/object/status/date/timezone/coordinate/illumination/phase/solar ranges are rejected; 262145-byte response is rejected with a fake response object; polar day/night and null events normalize without manufactured timestamps; and neutral fallback exact constants. Use only local fixtures and monkeypatch/injection—opening a real URL must fail the test.</action>
  <verify>python3 -m unittest -v tests/test_pixel_city_dynamic.py</verify>
  <acceptance_criteria>
    - Test names explicitly include `same_day`, `failed_attempt`, `stale`, `manual`, `oversized`, `polar`, `null`, `fallback`, and `copied_config` cases.
    - A guard test patches `urllib.request.urlopen` to raise if any non-fixture network path is reached.
    - `python3 -m unittest -v tests/test_pixel_city_dynamic.py` exits 0.
  </acceptance_criteria>
  <done>All non-concurrent production and validation invariants are covered without network or Wayland.</done>
</task>

<task type="auto">
  <name>Task 2: Prove process-shared once-daily reservation</name>
  <files>tests/test_pixel_city_dynamic.py</files>
  <read_first>tests/test_pixel_city_dynamic.py, examples/pixel-city-dynamic/dynamic_scene.py</read_first>
  <action>Add a multiprocessing-safe test using two child processes, one shared cache path, a start barrier/event, and a file or synchronized counter fetcher. Both children request the same provider/date. The first fetch sleeps briefly while holding the cache lock; the second blocks, reloads the reserved cache, and does not fetch. Assert both exit 0, the shared fetch count is exactly 1, the cache JSON parses at schema version 1, and a separate same-day failed fetch competition also totals exactly 1. Avoid timing-only pass conditions: use explicit synchronization and bounded joins that terminate/fail stuck children.</action>
  <verify>python3 -m unittest -v tests.test_pixel_city_dynamic.DailyCacheConcurrencyTests</verify>
  <acceptance_criteria>
    - Success-race test asserts the shared provider fetch count equals exactly 1.
    - Failure-race test asserts the shared provider fetch count equals exactly 1.
    - Children use bounded joins and the test fails/terminates any process still alive.
    - Resulting cache file parses and retains `schema_version == 1`.
  </acceptance_criteria>
  <done>Real processes prove the lock and reservation order enforce the same-day ceiling for success and failure.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate the Phase 1 suite with repository script tests</name>
  <files>tests/test_pixel_city_dynamic.sh</files>
  <read_first>Makefile, tests/run_tests.sh, tests/test_pixel_city_dynamic.py, .planning/codebase/TESTING.md</read_first>
  <action>Create an executable POSIX/Bash wrapper named `tests/test_pixel_city_dynamic.sh` that resolves repository root from its own path, runs `python3 -m unittest -v tests/test_pixel_city_dynamic.py`, separately runs the TOML/layer existence assertion, and propagates failures. Match the existing `test_*.sh` discovery convention so `make test-scripts` executes it. Do not edit Makefile unless current discovery demonstrably excludes the wrapper.</action>
  <verify>bash -n tests/test_pixel_city_dynamic.sh &amp;&amp; tests/test_pixel_city_dynamic.sh &amp;&amp; make test-scripts</verify>
  <acceptance_criteria>
    - Wrapper is executable and `bash -n` exits 0.
    - Direct wrapper execution exits 0 and prints unittest results.
    - `make test-scripts` output includes `test_pixel_city_dynamic.sh` and exits 0.
  </acceptance_criteria>
  <done>Phase 1 validation is available both directly and through the repository script-test target.</done>
</task>

</tasks>

<verification>
- `python3 -m unittest -v tests/test_pixel_city_dynamic.py`
- `tests/test_pixel_city_dynamic.sh`
- `make test-scripts`
- `python3 -m py_compile examples/pixel-city-dynamic/dynamic_scene.py tests/test_pixel_city_dynamic.py`
- `git diff --check`
</verification>

<success_criteria>
- All eight Phase 1 requirement IDs have automated direct evidence.
- Same-date success, failure, and two-process races each prove at most one fetch.
- No validation command requires network, Wayland, or the Hyprlax daemon.
</success_criteria>

<output>
After completion, create `.planning/phases/01-daily-data-foundation/01-02-SUMMARY.md` with coverage,
verification evidence, deviations, and Lore commit hash.
</output>
