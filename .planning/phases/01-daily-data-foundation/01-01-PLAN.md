---
phase: 01-daily-data-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/pixel-city-dynamic/1.png
  - examples/pixel-city-dynamic/2.png
  - examples/pixel-city-dynamic/3.png
  - examples/pixel-city-dynamic/4.png
  - examples/pixel-city-dynamic/5.png
  - examples/pixel-city-dynamic/6.png
  - examples/pixel-city-dynamic/parallax.toml
  - examples/pixel-city-dynamic/dynamic_scene.py
autonomous: true
requirements:
  - BASE-01
  - BASE-02
  - GEO-01
  - GEO-02
  - ASTRO-01
  - ASTRO-02
  - CACHE-01
  - CACHE-02
must_haves:
  truths:
    - "The copied six-layer Pixel City TOML parses and leaves the original directory unchanged"
    - "A provider category is reserved under a filesystem lock before its single daily network attempt"
    - "Manual coordinates bypass ip-api and astronomy uses one combined daily response"
    - "Last-good data survives failed refreshes and empty cache has a deterministic neutral fallback"
  artifacts:
    - path: examples/pixel-city-dynamic/parallax.toml
      provides: Valid copied six-layer base scene
      contains: "path = \"./6.png\""
    - path: examples/pixel-city-dynamic/dynamic_scene.py
      provides: Standard-library daily cache, HTTP, provider validation, and fallback module
      contains: "class DailyCache"
  key_links:
    - from: examples/pixel-city-dynamic/dynamic_scene.py
      to: ip-api.com
      via: LocationProvider minimal fields URL
      pattern: "ip-api.com/json"
    - from: examples/pixel-city-dynamic/dynamic_scene.py
      to: api.sunrise-sunset.org/v2
      via: AstronomyProvider daily query
      pattern: "api.sunrise-sunset.org/v2"
---

<objective>
Create the valid copied scene and implement the complete dependency-free daily input boundary that
later visual phases can call without knowing about provider/cache details.

Purpose: lock privacy, request ceilings, validation, concurrency, and offline behavior before any
scene animation depends on them.
Output: copied six-layer example plus importable `dynamic_scene.py` data foundation.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-daily-data-foundation/01-CONTEXT.md
@.planning/phases/01-daily-data-foundation/01-RESEARCH.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Establish the valid copied Pixel City base</name>
  <files>examples/pixel-city-dynamic/1.png, examples/pixel-city-dynamic/2.png, examples/pixel-city-dynamic/3.png, examples/pixel-city-dynamic/4.png, examples/pixel-city-dynamic/5.png, examples/pixel-city-dynamic/6.png, examples/pixel-city-dynamic/parallax.toml</files>
  <read_first>examples/pixel-city/parallax.toml, examples/pixel-city/README.md, .planning/phases/01-daily-data-foundation/01-CONTEXT.md</read_first>
  <action>Copy the six runtime PNG layers from `examples/pixel-city` into a new `examples/pixel-city-dynamic` directory. Copy the TOML structure but set the final blur to the concrete valid value `0.0` (never `0.0VV`), retain `fps = 144`, `duration = 4.0`, `easing = "expo"`, `global.render.tile = { x = true, y = false }`, workspace input, `shift_percent = 5`, and the six multipliers/blur values. Do not edit any file in `examples/pixel-city`. Do not copy unused `7.png`, `10.png`, or 1x1 documentation placeholders.</action>
  <verify>python3 -c 'import pathlib,tomllib; p=pathlib.Path("examples/pixel-city-dynamic/parallax.toml"); d=tomllib.loads(p.read_text()); assert len(d["global"]["layers"]) == 6; assert all((p.parent / x["path"]).is_file() for x in d["global"]["layers"])' &amp;&amp; git diff --exit-code -- examples/pixel-city</verify>
  <acceptance_criteria>
    - `examples/pixel-city-dynamic/parallax.toml` parses through Python `tomllib`.
    - The parsed `global.layers` length is exactly 6 and every relative path exists.
    - `rg -n '0\.0VV' examples/pixel-city-dynamic` returns no matches.
    - `git diff --exit-code -- examples/pixel-city` exits 0.
  </acceptance_criteria>
  <done>The copied base is valid, self-contained for its six runtime layers, and the original scene is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Implement locked atomic cache and bounded JSON HTTP boundary</name>
  <files>examples/pixel-city-dynamic/dynamic_scene.py</files>
  <read_first>.planning/phases/01-daily-data-foundation/01-RESEARCH.md, examples/cherry-blossom/time_overlay.py, src/ipc.c</read_first>
  <action>Create an importable Python 3.9+ module with `CACHE_SCHEMA_VERSION = 1`, `MAX_RESPONSE_BYTES = 262144`, `HTTP_TIMEOUT_SECONDS = 10`, `CacheError`, `ProviderError`, `DailyCache`, and `HttpJsonClient`. `DailyCache` defaults to `${XDG_CACHE_HOME:-~/.cache}/hyprlax/pixel-city-dynamic/daily-v1.json`, uses sibling `daily-v1.lock`, `fcntl.flock(LOCK_EX)`, mode 0600, JSON schema validation, temp file in the same directory, flush + `os.fsync`, `os.replace`, and best-effort directory fsync. Its reservation API must write `last_attempt_date` and `last_attempt_at` atomically before invoking the supplied fetch callback while holding the lock, preserve `last_success` independently, record a bounded error, and never retry internally. Reservation is keyed only by provider category plus resolved local date: a changed location key on the same date cannot permit a second attempt. Store a location key on `last_success` for reuse validation, and return neutral fallback rather than wrong-place data when it mismatches. After successful first-time geolocation, normalize its attempt date to the returned IANA timezone before releasing the lock. `HttpJsonClient.get_json(url)` must use an identifying Hyprlax GitHub User-Agent, 10 second timeout, read at most 262145 bytes and reject over 262144, require a JSON object, and surface HTTP/network/JSON errors as `ProviderError` without executing content.</action>
  <verify>python3 -m py_compile examples/pixel-city-dynamic/dynamic_scene.py &amp;&amp; python3 -c 'import importlib.util,pathlib,sys; p=pathlib.Path("examples/pixel-city-dynamic/dynamic_scene.py"); s=importlib.util.spec_from_file_location("dynamic_scene",p); m=importlib.util.module_from_spec(s); sys.modules[s.name]=m; s.loader.exec_module(m); assert m.CACHE_SCHEMA_VERSION == 1; assert m.MAX_RESPONSE_BYTES == 262144'</verify>
  <acceptance_criteria>
    - Module defines `DailyCache`, `HttpJsonClient`, `ProviderError`, `CACHE_SCHEMA_VERSION = 1`, `MAX_RESPONSE_BYTES = 262144`, and `HTTP_TIMEOUT_SECONDS = 10`.
    - Source contains `fcntl.flock`, `os.fsync`, and `os.replace`.
    - Attempt metadata is persisted before the injected fetch callback can execute.
    - There is no third-party import and no retry loop around provider I/O.
  </acceptance_criteria>
  <done>The cache/HTTP boundary enforces process-shared daily reservation and crash-safe last-good storage.</done>
</task>

<task type="auto">
  <name>Task 3: Implement validated location, astronomy, and neutral fallback resolution</name>
  <files>examples/pixel-city-dynamic/dynamic_scene.py</files>
  <read_first>examples/pixel-city-dynamic/dynamic_scene.py, .planning/research/STACK.md, .planning/phases/01-daily-data-foundation/01-CONTEXT.md</read_first>
  <action>Add immutable dataclasses for normalized `Location`, `Astronomy`, and `DailyFacts`; validators that reject booleans/non-finite/out-of-range coordinates, invalid `ZoneInfo`, wrong requested date/tz, malformed ISO event timestamps, unknown `sun_status`, unknown moon phase, illumination outside 0..100, and invalid solar azimuth/altitude. `LocationProvider` must call exactly `http://ip-api.com/json/?fields=status,message,lat,lon,timezone,city,regionName,country,countryCode`, validate JSON `status == "success"`, and support a manual `(latitude, longitude, timezone, locality)` path that performs zero ip-api calls. `AstronomyProvider` must make one encoded `https://api.sunrise-sunset.org/v2` request with `lat`, `lng`, and explicit `date`, normalizing both solar and lunar fields including documented nulls. `resolve_daily_facts` must orchestrate the two provider categories through `DailyCache`, store coordinates/timezone as astronomy success identity, suppress all second same-date astronomy attempts even when identity changes, reuse cached astronomy only when identity matches, mark stale sources, and fall back to 05:30 civil dawn, 06:00 sunrise, 12:00 solar noon, 18:00 sunset, 18:30 civil dusk, null lunar events, `New Moon`, 0% illumination, and `source="fallback"` when nothing usable exists.</action>
  <verify>python3 -m py_compile examples/pixel-city-dynamic/dynamic_scene.py &amp;&amp; python3 -c 'import ast,pathlib; p=pathlib.Path("examples/pixel-city-dynamic/dynamic_scene.py"); t=ast.parse(p.read_text()); mods={n.names[0].name.split(".")[0] for n in ast.walk(t) if isinstance(n,ast.Import)}|{n.module.split(".")[0] for n in ast.walk(t) if isinstance(n,ast.ImportFrom) and n.module}; assert not (mods-{"argparse","dataclasses","datetime","fcntl","json","math","os","pathlib","tempfile","typing","urllib","zoneinfo"})'</verify>
  <acceptance_criteria>
    - `LocationProvider` source contains the exact minimal ip-api fields and manual override branch.
    - `AstronomyProvider` source contains the v2 HTTPS endpoint and a single combined solar/lunar parser.
    - Validators accept null polar/lunar events but reject wrong date, bad IANA zone, invalid ranges, and unknown phases.
    - Fallback constants exactly match 05:30, 06:00, 12:00, 18:00, 18:30, New Moon, and 0%.
    - Only approved Python standard-library top-level imports are present.
  </acceptance_criteria>
  <done>Callers receive one normalized daily fact record from manual, fresh, stale, or deterministic fallback sources.</done>
</task>

</tasks>

<verification>
- `python3 -m py_compile examples/pixel-city-dynamic/dynamic_scene.py`
- TOML parse/layer existence one-liner exits 0.
- `git diff --exit-code -- examples/pixel-city` exits 0.
- `git diff --check` exits 0.
</verification>

<success_criteria>
- All eight Phase 1 requirement IDs are implemented in production boundaries ready for direct tests.
- No live provider call is required to import, compile, or validate the copied example.
- No dependency or C-source change is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/01-daily-data-foundation/01-01-SUMMARY.md` with files,
decisions, deviations, verification evidence, and Lore commit hash.
</output>
