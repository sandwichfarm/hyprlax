---
phase: 04-safe-ipc-controller
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [examples/pixel-city-dynamic/dynamic_scene.py, tests/test_pixel_city_dynamic.py]
autonomous: true
requirements: [IPC-01, IPC-02, CLI-01, OPS-01]
must_haves:
  truths:
    - "Dry-run fixed-time preview performs no network/socket/file writes and prints inspectable JSON commands"
    - "Once/loop/status/manual/interval/error paths are bounded and testable"
    - "Mock IPC proves unrelated layers remain untouched and failures propagate"
---

<objective>Deliver the operator CLI/loop and prove the safe IPC contract through injected mock execution.</objective>
<tasks>
<task type="auto"><name>Task 1: Implement deterministic CLI, one-shot/status, and bounded loop lifecycle</name><files>examples/pixel-city-dynamic/dynamic_scene.py</files><read_first>examples/pixel-city-dynamic/dynamic_scene.py, .planning/phases/04-safe-ipc-controller/04-CONTEXT.md</read_first><action>Add `main(argv=None)`, argparse modes/validation, aware ISO parser, dry-run neutral facts/assumed IDs/no writes, live daily resolution, status JSON, once application, and loop with monotonic 15..3600 interval, KeyboardInterrupt clean exit, per-tick actionable error logging, and next-tick retry only. Replace the Phase 1 placeholder __main__ exit.</action><verify>python3 examples/pixel-city-dynamic/dynamic_scene.py --dry-run --at 2026-07-12T12:00:00+02:00 --latitude 47.4979 --longitude 19.0402 --timezone Europe/Budapest</verify><acceptance_criteria>Dry-run exits 0 with source=preview and JSON commands, no cache/generated mtime/socket dependency; invalid interval/naive time/partial location exit nonzero; `--help` documents all modes.</acceptance_criteria><done>The sidecar has inspectable deterministic and live operational modes.</done></task>
<task type="auto"><name>Task 2: Add mock-list/modify, ownership, delta, CLI, and failure tests</name><files>tests/test_pixel_city_dynamic.py</files><read_first>examples/pixel-city-dynamic/dynamic_scene.py, tests/test_pixel_city_dynamic.py</read_first><action>Add `IPCControllerTests` using temp canonical example copies and injected runners/controllers. Cover valid list with unrelated layer, duplicate/missing/malformed/whitespace, exact supported commands/order, no clear/saturation/restart, second-state zero deltas, subprocess timeout/nonzero, fixed-time dry-run no network/socket/writes, CLI validation, once apply, and loop retry boundary without real sleeping.</action><verify>python3 -m unittest -v tests.test_pixel_city_dynamic.IPCControllerTests &amp;&amp; tests/test_pixel_city_dynamic.sh</verify><acceptance_criteria>Every ownership/error/CLI case passes; unrelated id absent from all argv; dry-run is side-effect-free; full prior suite remains green.</acceptance_criteria><done>Runtime behavior has deterministic integration coverage and failure propagation.</done></task>
</tasks>
<verification>Run full tests, wrapper, dry-run readback, py_compile, help, and git diff --check.</verification>
<output>Create 04-02-SUMMARY.md and proceed to verifier/live smoke.</output>

