---
phase: 04-safe-ipc-controller
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [examples/pixel-city-dynamic/dynamic_scene.py]
autonomous: true
requirements: [IPC-01, IPC-02, CLI-01, OPS-01]
must_haves:
  truths:
    - "Canonical discovery owns exactly the nine example layers and ignores unrelated daemon state"
    - "Command plans use only supported one-property modify operations and switch inactive assets before opacity"
    - "Repeated identical state produces zero commands"
  artifacts:
    - path: examples/pixel-city-dynamic/dynamic_scene.py
      provides: IPC adapter, ownership, command planner, delta controller
      contains: "class HyprlaxIPC"
---

<objective>Connect verified daily state/assets to current Hyprlax IPC with strict ownership and delta-only mutation.</objective>
<tasks>
<task type="auto"><name>Task 1: Implement subprocess IPC adapter and canonical managed-layer discovery</name><files>examples/pixel-city-dynamic/dynamic_scene.py</files><read_first>examples/pixel-city-dynamic/dynamic_scene.py, src/ipc.c, src/hyprlax_ctl.c, .planning/phases/04-safe-ipc-controller/04-CONTEXT.md</read_first><action>Add `IPCError`, frozen managed-layer types, `HyprlaxIPC.list_layers/modify`, strict JSON/range validation, canonical expected path matching, whitespace rejection, missing/duplicate rejection, and unrelated-layer ignore. Runner and timeout are injectable; failures include command and stderr.</action><verify>python3 -m py_compile examples/pixel-city-dynamic/dynamic_scene.py</verify><acceptance_criteria>Discovery returns six base plus sun/moon/shadow only; no hard-coded live IDs; unrelated IDs never enter mapping; malformed/missing/duplicate/whitespace fail.</acceptance_criteria><done>The controller can identify only its owned runtime layers and issue bounded modify calls.</done></task>
<task type="auto"><name>Task 2: Implement asset update, supported command plan, and delta-only execution</name><files>examples/pixel-city-dynamic/dynamic_scene.py</files><read_first>examples/pixel-city-dynamic/dynamic_scene.py, .planning/phases/04-safe-ipc-controller/04-RESEARCH.md</read_first><action>Add RGB hex formatting, managed inactive moon/shadow generation, exact command planner for base tint/opacity/blur and dynamic path/x/y/opacity, assumed dry-run mapping, `CommandDelta`, and `SceneController.apply_once`. Write assets before commands; update delta signatures only after each successful modify; never emit clear/restart/saturation/add/remove.</action><verify>python3 -m py_compile examples/pixel-city-dynamic/dynamic_scene.py</verify><acceptance_criteria>Plans target managed IDs only; path precedes opacity; identical second plan filters to zero; every property is in path/x/y/opacity/tint/blur.</acceptance_criteria><done>One SceneState safely becomes minimal current-IPC mutations.</done></task>
</tasks>
<verification>Run py_compile, current full tests, and git diff --check.</verification>
<output>Create 04-01-SUMMARY.md and Lore commit.</output>

