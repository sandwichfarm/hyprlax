---
phase: 03-pixel-sky-and-shadows
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [tests/test_pixel_city_dynamic.py]
autonomous: true
requirements: [ASSET-01, SHADOW-01, ASSET-02]
must_haves:
  truths:
    - "Tests independently decode and measure every generated visual invariant"
    - "Tests prove shadow direction, strength, absence, and buffer alternation"
  artifacts:
    - path: tests/test_pixel_city_dynamic.py
      provides: PNG, moon, shadow, and buffering regression coverage
      contains: "class GeneratedAssetTests"
---

<objective>Prove generated pixels and atomic reload behavior independently of the renderer.</objective>
<tasks>
<task type="auto"><name>Task 1: Test codec, sun, phase geometry, and committed initial assets</name><files>tests/test_pixel_city_dynamic.py</files><read_first>examples/pixel-city-dynamic/dynamic_scene.py, tests/test_pixel_city_dynamic.py, examples/pixel-city-dynamic/parallax.toml</read_first><action>Add `GeneratedAssetTests` for RGBA roundtrip, corrupt/unsupported rejection, source decode, sun alpha, new/quarter/full bright counts, waxing/waning centroid sides, initial asset decode, and exact nine-layer config order.</action><verify>python3 -m unittest -v tests.test_pixel_city_dynamic.GeneratedAssetTests</verify><acceptance_criteria>All codec and celestial geometry assertions pass with independent decoded pixel measurements.</acceptance_criteria><done>Celestial images and tracked placeholders have direct pixel evidence.</done></task>
<task type="auto"><name>Task 2: Test projected shadow direction/elevation/absence and atomic A/B identities</name><files>tests/test_pixel_city_dynamic.py</files><read_first>examples/pixel-city-dynamic/dynamic_scene.py, tests/test_pixel_city_dynamic.py</read_first><action>Render matched morning/afternoon/low/noon/night states from 6.png; compare alpha centroids/sums and zero night alpha. Write three buffers through `DoubleBufferedAssets`, assert a→b→a identity and valid decode after each atomic replacement.</action><verify>python3 -m unittest -v tests.test_pixel_city_dynamic.GeneratedAssetTests &amp;&amp; tests/test_pixel_city_dynamic.sh</verify><acceptance_criteria>Morning centroid lies right of afternoon; low-sun alpha sum exceeds noon; hidden sun sum is zero; buffer identity alternates and decodes.</acceptance_criteria><done>Shadow and reload invariants are regression-locked.</done></task>
</tasks>
<verification>Run full tests, script wrapper, py_compile, PNG/TOML readback, and git diff --check.</verification>
<output>Create 03-02-SUMMARY.md and proceed to verifier.</output>

